var data = new Date();
   var ano = data.getFullYear(); // Obtém o ano (ex: 2023)
   var mes = data.getMonth(); // Obtém o mês (0-11, sendo 0 para janeiro)
   var dia = data.getDate(); // Obtém o dia do mês (1-31)
   var hora = data.getHours(); // Obtém a hora (0-23)
   var minuto = data.getMinutes(); // Obtém os minutos (0-59)
   var segundo = data.getSeconds(); // Obtém os segundos (0-59)
   