function soma(a, b) {
    return a + b;
  }
  
  var resultado = soma(2, 3); // Chama a função e atribui o resultado a uma variável
  console.log(resultado); // Exibe o valor retornado pela função (5)
  